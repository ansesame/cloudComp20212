CREATE TABLE IF NOT EXISTS heart_obs (
    index INTEGER PRIMARY KEY,
    age INTEGER,
    sex INTEGER,
    cp INTEGER,
    trestbps INTEGER,
    chol INTEGER,
    fbs INTEGER,
    restecg INTEGER,
    thalach INTEGER,
    exang INTEGER,
    oldpeak NUMERIC,
    slope INTEGER,
    ca INTEGER,
    thal INTEGER,
    target INTEGER
);
